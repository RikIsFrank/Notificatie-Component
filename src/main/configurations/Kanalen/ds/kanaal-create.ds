local base = "http://localhost/api/v1/kanaal/";

{
  url: base + payload.uuid,
  naam: payload.originalMessage.naam,
  documentatieLink: payload.originalMessage.documentatieLink,
  filters: payload.originalMessage.filters,
}