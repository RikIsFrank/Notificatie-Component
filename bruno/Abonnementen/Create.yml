info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{ROOT_URI}}/api/{{API_VERSION}}/abonnement"
  body:
    type: json
    data: |-
      {
          "callbackUrl": "https://webhook.site/0ef19537-521a-41e4-a2c9-c3bfbf86a392",
          "auth": "Bearer test-token-abc123",
          "kanalen": [
              {
                  "naam": "test 2",
                  "filters": {
                      "property1": "string",
                      "property2": "string"
                  }
              }
          ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
