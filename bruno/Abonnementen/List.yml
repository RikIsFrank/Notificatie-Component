info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{ROOT_URI}}/api/{{API_VERSION}}/abonnement"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
