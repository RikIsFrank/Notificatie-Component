info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{ROOT_URI}}/api/{{API_VERSION}}/notificaties"
  body:
    type: json
    data: |-
      {
        "kanaal": "test 2",
        "hoofdObject": "http://example.com",
        "resource": "string",
        "resourceUrl": "http://example.com",
        "actie": "string",
        "aanmaakdatum": "2019-08-24T14:15:22Z",
        "kenmerken": {
          "property1": "string",
          "property2": "string"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
