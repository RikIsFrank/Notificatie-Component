info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{ROOT_URI}}/api/{{API_VERSION}}/kanaal"
  body:
    type: json
    data: |-
      {
        "naam": "test 2",  
        "documentatieLink": "http://example.com",
          "filters": [
              "test",
              "extra filter"
          ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
